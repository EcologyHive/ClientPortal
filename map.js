window.SR=window.SR||{};(function(ns){function destPoint(lat,lon,bearingDeg,distM){const R=6371e3;const brng=bearingDeg*Math.PI/180,lat1=lat*Math.PI/180,lon1=lon*Math.PI/180;const lat2=Math.asin(Math.sin(lat1)*Math.cos(distM/R)+Math.cos(lat1)*Math.sin(distM/R)*Math.cos(brng));const lon2=lon1+Math.atan2(Math.sin(brng)*Math.sin(distM/R)*Math.cos(lat1),Math.cos(distM/R)-Math.sin(lat1)*Math.sin(lat2));return[lat2*180/Math.PI,lon2*180/Math.PI]}function wedgePolygon(lat,lon,bearingDeg,fovDeg,radiusM,steps=10){const points=[[lat,lon]];const start=bearingDeg-fovDeg/2,end=bearingDeg+fovDeg/2;for(let i=0;i<=steps;i++)points.push(destPoint(lat,lon,start+(end-start)*(i/steps),radiusM));points.push([lat,lon]);return points}function bearingBetween(lat1,lon1,lat2,lon2){const φ1=lat1*Math.PI/180,φ2=lat2*Math.PI/180,Δλ=(lon2-lon1)*Math.PI/180;const y=Math.sin(Δλ)*Math.cos(φ2);const x=Math.cos(φ1)*Math.sin(φ2)-Math.sin(φ1)*Math.cos(φ2)*Math.cos(Δλ);return(Math.atan2(y,x)*180/Math.PI+360)%360}function divIcon(emoji,opts){return L.divIcon({html:`<div style="font-size:20px;line-height:1;filter:drop-shadow(0 1px 2px rgba(0,0,0,.6))">${emoji}</div>`,className:"sr-map-icon",iconSize:[24,24],iconAnchor:[12,12],...opts})}const ICON_EMOJI={location:"📷",detector:"🎤",pin:"⭐",roost:"🦇"};function iconFor(kind,opts){return divIcon(ICON_EMOJI[kind]||ICON_EMOJI.pin,opts)}function makeTileLayer(key){return key==="streets"?L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{maxZoom:21,attribution:"© OpenStreetMap"}):L.tileLayer("https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}",{maxZoom:21,attribution:"Imagery © Esri"})}const DEFAULT_BASE_LAYER="satellite";ns.Map={destPoint:destPoint,wedgePolygon:wedgePolygon,bearingBetween:bearingBetween,divIcon:divIcon,iconFor:iconFor,makeTileLayer:makeTileLayer,DEFAULT_BASE_LAYER:DEFAULT_BASE_LAYER}})(window.SR);