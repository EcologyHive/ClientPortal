window.SR=window.SR||{};(function(ns){function fftInPlace(re,im){const n=re.length;for(let i=1,j=0;i<n;i++){let bit=n>>1;for(;j&bit;bit>>=1)j^=bit;j^=bit;if(i<j){let t=re[i];re[i]=re[j];re[j]=t;t=im[i];im[i]=im[j];im[j]=t}}for(let len=2;len<=n;len<<=1){const ang=-2*Math.PI/len;const wr=Math.cos(ang),wi=Math.sin(ang);for(let i=0;i<n;i+=len){let curWr=1,curWi=0;for(let k=0;k<len/2;k++){const uRe=re[i+k],uIm=im[i+k];const vRe=re[i+k+len/2]*curWr-im[i+k+len/2]*curWi;const vIm=re[i+k+len/2]*curWi+im[i+k+len/2]*curWr;re[i+k]=uRe+vRe;im[i+k]=uIm+vIm;re[i+k+len/2]=uRe-vRe;im[i+k+len/2]=uIm-vIm;const nextWr=curWr*wr-curWi*wi;curWi=curWr*wi+curWi*wr;curWr=nextWr}}}}function hannWindow(size){const w=new Float32Array(size);for(let i=0;i<size;i++)w[i]=.5-.5*Math.cos(2*Math.PI*i/(size-1));return w}const MAX_ANALYSIS_DURATION_SEC=30;function computeSpectrogram(samples,sampleRate,fftSize,hopSize,maxDurationSec){hopSize=hopSize||Math.floor(fftSize/2);maxDurationSec=maxDurationSec==null?MAX_ANALYSIS_DURATION_SEC:maxDurationSec;const maxSamples=Math.floor(maxDurationSec*sampleRate);const truncated=samples.length>maxSamples;if(truncated)samples=samples.subarray(0,maxSamples);const window=hannWindow(fftSize);const numBins=fftSize/2+1;const numFrames=Math.max(1,Math.floor((samples.length-fftSize)/hopSize)+1);const magDb=new Float32Array(numFrames*numBins);const frameTimes=new Float32Array(numFrames);const re=new Float64Array(fftSize);const im=new Float64Array(fftSize);const EPS=1e-9;for(let f=0;f<numFrames;f++){const start=f*hopSize;for(let i=0;i<fftSize;i++){const s=start+i<samples.length?samples[start+i]:0;re[i]=s*window[i];im[i]=0}fftInPlace(re,im);for(let b=0;b<numBins;b++){const mag=Math.sqrt(re[b]*re[b]+im[b]*im[b])/fftSize;magDb[f*numBins+b]=20*Math.log10(mag+EPS)}frameTimes[f]=(start+fftSize/2)/sampleRate}const freqs=new Float32Array(numBins);for(let b=0;b<numBins;b++)freqs[b]=b*sampleRate/fftSize;return{numFrames:numFrames,numBins:numBins,magDb:magDb,frameTimes:frameTimes,freqs:freqs,sampleRate:sampleRate,fftSize:fftSize,hopSize:hopSize,durationSec:samples.length/sampleRate,truncated:truncated}}function frameIndexForTime(spec,t){if(spec.numFrames<=1)return 0;const dt=spec.frameTimes[1]-spec.frameTimes[0];const idx=Math.round((t-spec.frameTimes[0])/dt);return Math.min(spec.numFrames-1,Math.max(0,idx))}function binIndexForFreq(spec,freqHz){const dHz=spec.freqs[1]-spec.freqs[0];const idx=Math.round(freqHz/dHz);return Math.min(spec.numBins-1,Math.max(0,idx))}function hslToRgb(h,s,l){h=(h%360+360)%360;const c=(1-Math.abs(2*l-1))*s;const x=c*(1-Math.abs(h/60%2-1));const m=l-c/2;let r1,g1,b1;if(h<60){r1=c;g1=x;b1=0}else if(h<120){r1=x;g1=c;b1=0}else if(h<180){r1=0;g1=c;b1=x}else if(h<240){r1=0;g1=x;b1=c}else if(h<300){r1=x;g1=0;b1=c}else{r1=c;g1=0;b1=x}return[Math.round((r1+m)*255),Math.round((g1+m)*255),Math.round((b1+m)*255)]}function makeColorLut(floorDb,rangeDb,saturation){const steps=256;const lut=new Uint8ClampedArray(steps*3);for(let i=0;i<steps;i++){const norm=i/(steps-1);const hue=220-norm*205;const lightness=.04+norm*.62;const[r,g,b]=hslToRgb(hue,saturation,lightness);lut[i*3]=r;lut[i*3+1]=g;lut[i*3+2]=b}return lut}function dbToLutIndex(db,floorDb,rangeDb){const norm=(db-floorDb)/rangeDb;return Math.min(255,Math.max(0,Math.round(norm*255)))}const MAX_DISPLAY_COLUMNS=4e3;function renderSpectrogramImageData(spec,opts){const{frameFrom:frameFrom,frameTo:frameTo,binFrom:binFrom,binTo:binTo,floorDb:floorDb,rangeDb:rangeDb,saturation:saturation}=opts;const totalFrames=frameTo-frameFrom+1;const w=Math.min(MAX_DISPLAY_COLUMNS,totalFrames);const h=binTo-binFrom+1;const framesPerCol=totalFrames/w;const lut=makeColorLut(floorDb,rangeDb,saturation);const img=new ImageData(w,h);for(let c=0;c<w;c++){const groupStart=frameFrom+Math.floor(c*framesPerCol);const groupEnd=Math.max(groupStart+1,frameFrom+Math.floor((c+1)*framesPerCol));for(let b=0;b<h;b++){const binIdx=binFrom+b;let maxDb=-Infinity;for(let frameIdx=groupStart;frameIdx<groupEnd;frameIdx++){const db=spec.magDb[frameIdx*spec.numBins+binIdx];if(db>maxDb)maxDb=db}const lutIdx=dbToLutIndex(maxDb,floorDb,rangeDb);const py=h-1-b;const pixelOffset=(py*w+c)*4;img.data[pixelOffset]=lut[lutIdx*3];img.data[pixelOffset+1]=lut[lutIdx*3+1];img.data[pixelOffset+2]=lut[lutIdx*3+2];img.data[pixelOffset+3]=255}}return img}const DEFAULT_FLOOR_DB=-70;ns.Dsp={DEFAULT_FLOOR_DB:DEFAULT_FLOOR_DB,computeSpectrogram:computeSpectrogram,frameIndexForTime:frameIndexForTime,binIndexForFreq:binIndexForFreq,renderSpectrogramImageData:renderSpectrogramImageData}})(window.SR);